```fig
title = Closed tasks
part_of = [Tasks](/docs/tasks/tasks.md)
contents = [[`fig delete` never reaches `deleteContainer`, so a section, table or block cannot be deleted from the command line](/docs/tasks/closed/cli-delete-never-reaches-delete-container.md), '[A command-line path cannot name a key that contains a `.` or a `[`](/docs/tasks/closed/cli-path-cannot-quote-a-dotted-key.md)', [Editor: read and write the dangling anchor, and comment a node out and back in](/docs/tasks/closed/dangling-comments-and-comment-out-ops.md), [Everything-on `zig build test` fails to compile](/docs/tasks/closed/everything-on-test-build-fails.md), [A flow sequence item reports, and deletes, its parent's leading comment](/docs/tasks/closed/flow-item-leading-comment-is-the-parents.md), [An insert into a section with no entries of its own lands inside its first child](/docs/tasks/closed/insert-into-a-section-with-no-own-entries-lands-in-its-first-child.md), [JSON printer emits a non-string mapping key as invalid JSON](/docs/tasks/closed/json-printer-emits-non-string-keys.md), [`set` falls back to an insert on any replace error, so a refused replace becomes a duplicate entry](/docs/tasks/closed/set-falls-back-to-an-insert-on-any-replace-error.md), [YAML printer drops `%TAG` directives and root collection properties](/docs/tasks/closed/yaml-printer-drops-directives-and-root-props.md), [YAML printer panics on some accept-corpus documents](/docs/tasks/closed/yaml-printer-panics-on-accept-corpus.md), [The Rust and TypeScript editors spell a NestedText value through the printer, which is a `>` block](/docs/tasks/closed/rust-editor-spells-a-nestedtext-value-through-the-printer.md), [The Rust editor spells a key through the format's printer, which for plist is `<string>k</string>`](/docs/tasks/closed/rust-editor-spells-a-plist-key-through-the-printer.md)]
```

# Closed tasks

The tasks that say they are done or dropped, moved here once they said so.
Each still says what closed it; what is live stays in the index above this one.
